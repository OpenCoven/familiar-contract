# 04 With USER.md

This case demonstrates a familiar that carries an optional `USER.md` context file. Sage's Ward already treats `USER.md` as protected if present, so adding the file should not break compliance.
