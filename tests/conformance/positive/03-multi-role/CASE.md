# 03 Multi Role

This case demonstrates that a familiar may include role scaffolding beneath `roles/` when that path is part of the Ward's editable surface; the added role file does not alter the protected identity contract and should still validate.
