# 06 No Core Work

Violated property: Defined Purpose. Validator check: `validateSoul` should report `SOUL.md › core_work` because the `## Core Work` section is missing. Expected behavior: `node validators/validate.js <case>` exits non-zero with an error pattern like `No "## Core Work" section found`.
