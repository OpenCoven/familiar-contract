# 07 No What I Am Not

Violated property: Defined Purpose. Validator check: `validateSoul` should report `SOUL.md › what_i_am_not` because the `## What I Am Not` section is missing. Expected behavior: `node validators/validate.js <case>` exits non-zero with an error pattern like `No "## What I Am Not" section found`.
