# 01 Missing SOUL.md

Violated property: Named Identity. Validator check: `validateSoul` should report `SOUL.md › file` because `SOUL.md` is absent. Expected behavior: `node validators/validate.js <case>` exits non-zero with an error pattern like `SOUL.md does not exist`.
