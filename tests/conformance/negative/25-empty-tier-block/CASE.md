# 25 Empty Tier Block

Expected: FAIL. Empty-string harness block identifiers are forbidden even when `[editable].harness_blocks` and `approval_tiers.auto.blocks` both reference the same empty string.
